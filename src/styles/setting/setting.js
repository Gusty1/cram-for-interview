/**
 * 設定頁面的樣式設定
 */

import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  // 列的樣式
  settingRow: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-center'
  }
})
