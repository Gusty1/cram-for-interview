/**
 * 通用樣式設定
 */

import { StyleSheet } from "react-native";

export default StyleSheet.create({
  // 一般主要容器設定
  mainContainer: {
    flex: 1,
    marginTop: 10,
    paddingLeft: 10,
    paddingRight: 10,
  },
});
